angledcrosshatch.gcode
crosshatch.gcode
raster.gcode
spiral.gcode
isotropic.gcode
venv
octovenv/
.venv/
env/
__pycache__/
offset.gcode

# Local overrides / secrets -- never commit. Built-in defaults are the Pi's;
# ucei_local.json (see ucei_local.example.json) overrides them on Windows/remote.
ucei_local.json
